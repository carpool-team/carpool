/** User data to add in form interface */
export interface IFormUserData {
	/** Name of user to add */
	name: string;
	/** Surname of user to add */
	surname: string;
	/** Email of user to add */
	email: string;
}