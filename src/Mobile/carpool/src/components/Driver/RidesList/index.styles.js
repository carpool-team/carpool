import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  flatlist: {
    width: '100%',
    paddingTop: 18,
  },
  contentContainer: {
    paddingHorizontal: 16,
    paddingBottom: 72,
  },
});
