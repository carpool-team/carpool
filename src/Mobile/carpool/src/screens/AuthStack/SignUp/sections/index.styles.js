import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  scrollView: {
    alignItems: 'center',
    marginVertical: 20,
    paddingHorizontal: 32,
  },
  inputWrapper: {
    width: '100%',
    marginBottom: 40,
  },
  button: {
    marginTop: 20,
  },
});
