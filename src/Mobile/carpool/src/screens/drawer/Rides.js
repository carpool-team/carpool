import React from 'react';
import {View, Text} from 'react-native';

const Rides = () => {
  return (
    <View
      style={{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
      }}>
      <Text>Rides</Text>
    </View>
  );
};

export default Rides;
