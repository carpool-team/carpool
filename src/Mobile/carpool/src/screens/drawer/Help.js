import React from 'react';
import {View, Text} from 'react-native';

const Help = () => {
  return (
    <View
      style={{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
      }}>
      <Text>Help</Text>
    </View>
  );
};

export default Help;
