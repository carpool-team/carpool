import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  safeArea: {
    width: '100%',
    flex: 1,
  },
  flatlist: {
    width: '100%',
  },
  contentContainer: {
    paddingTop: 30,
    paddingHorizontal: 16,
  },
});
